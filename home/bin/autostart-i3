#!/bin/bash

setxkbmap -option compose:rctrl,ctrl:nocaps
autorandr -c
"$HOME/.fehbg"

/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
dunst &

barrier &

# /usr/lib/kdeconnectd &
# kdeconnect-indicator &

redshift -l "-20.3285:-40.372" -t 6500:2000 &
